# Research Trace — ONE-OFF DATASETS (AI-OSINT)

- Run type: out-of-cadence ONE-OFF DATASETS
- Slot: datasets
- UTC research window: 2026-03-06 17:22–17:28 UTC (~36 minutes)
- Constraints honored: no cadence state file read/write; no cadence rotation edits.

## Research objective
Find one **new STORY** candidate (not follow-up in last 14 days), pass AP publishability gate, and add >=1 new dataset source to catalog.

## Leads explored

### Lead A — EU measles trend (ECDC)
- Why considered: immediate public-health relevance with cross-border implications.
- Searches run:
  - `site:data.ecdc.europa.eu measles monthly cases 2026`
  - ECDC data-discovery attempts via web search.
- Datasets/endpoints checked:
  - ECDC public data site discovery results (no fast, reproducible endpoint confirmed during run window).
- Hypothesis notes:
  - Potentially strong if weekly/monthly confirmed-case feed is directly queryable.
- Rejection reason:
  - Retrieval reproducibility not strong enough in the available one-off window.

### Lead B — WHO FluNet seasonal turning-point story
- Why considered: high relevance, near-real-time respiratory signal.
- Searches run:
  - `WHO FluNet influenza positivity dashboard data download`
  - Fetch on WHO FluNet summary page.
- Datasets/endpoints checked:
  - WHO FluNet summary page (returned content last updated 2024 in fetched body).
- Hypothesis notes:
  - Could support "post-peak decline" if live machine endpoint is confirmed.
- Rejection reason:
  - Public summary content appeared stale in this retrieval path; confidence/reproducibility risk too high for publication today.

### Lead C — Sanctions data-feed synchronization risk (OFAC vs Canada vs UN)
- Why considered: actionable compliance relevance; direct primary-source machine feeds available now.
- Searches run:
  - DOL/FRED labor lead checks (benchmark alternative; eventually rejected as too weakly novel)
  - `UN Security Council Consolidated Sanctions List XML download`
- Datasets/endpoints checked:
  - OFAC Sanctions List Service SDN CSV export endpoint (headers + row counts)
  - Global Affairs Canada consolidated sanctions page + XML link
  - UN Security Council consolidated sanctions page + XML endpoint
- Hypothesis notes:
  1. Feed clocks are materially out of sync across authorities.
  2. Header timestamp and declared content-update date can diverge (especially Canada), creating implementation ambiguity.
  3. This is not just list-size mismatch; it is update-cadence mismatch.
- Advance reason:
  - Strong, directly reproducible primary sources; clear "why now" signal; novel OSINT contribution via cross-authority metadata triangulation.

## Candidate ranking outcome
1. **Selected (winning lead):** sanctions feed synchronization risk (OFAC/Canada/UN)
2. Rejected: WHO FluNet turning-point (stale retrieval path risk)
3. Rejected: ECDC measles (endpoint reproducibility gap)
4. Rejected: US labor claims divergence (signal too weak for strong AP now-test)

## Source classes touched
- Official government machine-readable sanctions feeds (US OFAC CSV, Canada XML)
- Intergovernmental sanctions registry (UNSC consolidated XML + official update text)
- Statistical/economic baseline APIs (FRED/DOL claims) for alternative lead testing

## Notes on novelty gate (14-day check)
- Recent 14-day corpus in `docs/` includes sanctions **dataset** additions and optimization notes, but no STORY centered on cross-authority sanctions feed-cadence divergence.
- Treated as a distinct claim family and evidence domain from prior entries.
